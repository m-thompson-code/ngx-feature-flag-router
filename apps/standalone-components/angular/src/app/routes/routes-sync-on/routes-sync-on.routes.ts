import { Routes } from '@angular/router';

import { RoutesSyncOnComponent } from './routes-sync-on.component';

export const routes: Routes = [
    {
        path: '',
        component: RoutesSyncOnComponent,
    },
];
