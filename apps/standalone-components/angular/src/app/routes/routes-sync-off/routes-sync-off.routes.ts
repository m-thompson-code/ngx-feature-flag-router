import { Routes } from '@angular/router';

import { RoutesSyncOffComponent } from './routes-sync-off.component';

export const routes: Routes = [
    {
        path: '',
        component: RoutesSyncOffComponent,
    },
];
