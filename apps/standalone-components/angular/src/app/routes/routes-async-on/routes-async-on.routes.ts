import { Routes } from '@angular/router';

import { RoutesAsyncOnComponent } from './routes-async-on.component';

export const routes: Routes = [
    {
        path: '',
        component: RoutesAsyncOnComponent,
    },
];
