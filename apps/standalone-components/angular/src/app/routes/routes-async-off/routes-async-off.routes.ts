import { Routes } from '@angular/router';

import { RoutesAsyncOffComponent } from './routes-async-off.component';

export const routes: Routes = [
    {
        path: '',
        component: RoutesAsyncOffComponent,
    },
];
