import { AngularVersion } from 'legacy-utilities';
import { Environment } from './environment.type';

export const environment: Environment = {
    angularVersions: [AngularVersion.source, AngularVersion.nine, AngularVersion.ten, AngularVersion.eleven, AngularVersion.twelve],
};
